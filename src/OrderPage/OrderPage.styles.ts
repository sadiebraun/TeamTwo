import { css } from "@emotion/css";

const styles = {
  wrapper: css({
    padding: 32,
    backgroundColor: "hotpink",
    fontSize: 24,
    borderRadius: 4,
    "&:hover": {
      color: "#fff"
    }
  })
};

export default styles;

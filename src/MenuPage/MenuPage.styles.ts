import { css } from "@emotion/css";

const styles = {
  wrapper: css({
    // backgroundColor: "hotpink",
  }),
  deliveryDateHeader: css({
    fontWeight: 400,
    fontSize: 40,
  }),
  menuGrid: css({
    display: "flex",
    flexWrap: "wrap",
    marginBottom: "2rem",
    padding: 0,
    listStyle: "none",
    justifyContent: "space-between",
  })
};

export default styles;
